# CLAUDE.md

このリポジトリで作業する Claude Code のためのプロジェクト情報。

## このファイルの育て方（Claudeへの指示）

- このファイルは「`/clear` などで会話がリセットされても重要な情報が失われないための保険」として機能する。セッション開始時に自動で読み込まれる。
- 作業がひと区切りついたら、そのたびに以下を追記すること:
  - 新しく分かった・決まったこと（プロジェクトの実態、ファイルの意味、ユーザーの意図など）
  - 作業中につまずいた点・注意が必要だと分かった点
  - まだ終わっていない残作業
- ユーザーから「CLAUDE.mdを更新して」と言われたら、その時点までの状況（分かったこと・やったこと・残作業）を反映して書き直すこと。
- 情報は「TODO」のまま放置せず、実際にファイルを読んで具体的に書くこと。
- このリポジトリは **public** リポジトリなので、個人情報・社外秘の取引条件（詳細な原価・仕入先個別交渉内容など）を不用意に書き足さないよう注意する。

## このプロジェクトは何か

コードは一切なく、アパレル（衣料品）メーカーの**商品開発・生産進捗管理**に使う
Excel資料一式を保管している資料庫リポジトリ。リポジトリ名 `conron-kannrisho` は
「コンロン（CONRON）管理書」を指すとみられる（ファイル中に品質表示タグの
「CONRON印字」という記載があり、取引先ブランド名と推測される）。

`GitHub投入済/` ディレクトリの下に、個別の商品（パンツ・デニム等）ごとの
`○商品進捗管理書○〇〇.xlsx` という命名のExcelファイルが並んでいる。
2026-06-01 に1コミットでまとめてアップロードされただけで、それ以降の更新履歴はない
（= このリポジトリ自体はほぼ「保管庫」としてのみ使われている）。

### 各Excelファイルの中身（実際に開いて確認済み）

典型的な「商品進捗管理書」ファイルは複数シート構成:

- **商品進捗管理表**（メインシート）: 得意先名、担当バイヤー、品番、品名、
  生地名・生地品番・混率、カラー展開、サイズ展開、股下展開、希望納期、
  販売条件、上代・下代、営業原価（BGN）、粗利率（BGN）、ボタン/リベット等の
  資材情報、品質表示（CONRON印字の可否）、生産工場（中国 / BGN）、備考など、
  1品番の企画〜生産条件を1枚にまとめた台帳形式。
- 見積用シート（例: `仮見積`、`○○見積`、`○○仮見積`）: 原価計算の詳細。
- 一部ファイルには追加で以下のようなシートもある:
  - `サイズ・付属` `縫製指示` `パーツリスト` `まとめ梱包` `検品`
    （`26sCR-P101_MensEasyPants_25.11.20.xlsx`）— 仕様書〜梱包〜検品まで
    より工程が細かく分かれたタイプ。
  - `決定ラベル` `ラベル付位置`（`26年夏【婦人冷感ブラックパンツストレート】.xlsx`）
    — 下げ札・ラベルのデザイン・貼付位置に関するシート。
  - `原纸`（`MK164N-31【デニム調】.xlsx`）— 生地の原反情報。

つまりこのリポジトリは「工程横断で使う進捗管理Excelのアーカイブ」であり、
アプリケーションコードや自動化スクリプトは今のところ存在しない。

## 主要ファイル・技術スタック

- 技術スタック: なし（コードリポジトリではない）。中身はすべて Microsoft Excel
  ファイル（`.xlsx` / 一部 `.xls`）。
- ディレクトリ構成:
  - `GitHub投入済/` — 唯一の格納ディレクトリ。名前の通り「GitHubに投入済み」の
    商品進捗管理書・見積書類が置かれている。サブディレクトリなし、フラット構成。
- 注意すべきファイル:
  - `~$○商品進捗管理書○【26年秋】816536(綿リヨセルデニムワイド).xlsx`
  - `~$○商品進捗管理書○【26年秋冬】らくばき裏起毛サテンストレート.xlsx`

    この2つは先頭が `~$` で、Excelがファイルを開いている間に作る**ロック/一時ファイル**
    であり、正規のExcelファイルではない（`openpyxl` で開こうとすると
    "File is not a zip file" エラーになることを確認済み）。誤ってコミットされたもの
    である可能性が高く、実体としては使い物にならない。ユーザーから指示があれば
    削除を検討してよいが、無断で消さないこと。

## 開発の進め方

- このリポジトリは「コードを書く」プロジェクトではなく、Excel資料を保管・整理する
  リポジトリとして扱われている（少なくとも現時点では）。
- git 運用は非常にシンプルで、`main` ブランチ1本、Web UI等からの
  "Add files via upload" によるファイル追加が主な更新手段になっている。
- 今後、進捗管理表からの集計・自動化（例: 複数ファイルを横断した一覧化、
  CSV化、Googleスプレッドシート連携など）を求められる可能性があるが、
  2026-09-18時点ではそうした仕組みは未着手。着手した場合はこのセクションを
  更新すること。

## 気づいた注意点・ハマりどころ

- ファイル名に `○` `【】` などの記号や全角文字が多く含まれるため、
  シェルやスクリプトで扱う際はクォート・エンコーディングに注意する
  （`git show --stat` の出力でも日本語ファイル名が8進数エスケープで
  表示されることがある。`git config core.quotepath false` などで
  可読化できる）。
- 同じ「商品進捗管理表」という名前のシートでも、ファイルによって
  レイアウト・追加シートの構成が微妙に異なる（担当者が案件ごとに
  コピーして作成していると見られる）ため、複数ファイルを機械的に
  横断集計する場合は列位置のズレを個別に確認する必要がある
  （※ Tag Extractor プロジェクトのCSV位置ズレ問題と同様の落とし穴が
  起こりうるので、集計スクリプトを書く際は決め打ちせず必ず実データで検証すること）。
- `~$` で始まるファイルはExcelのロックファイルであり中身が読めない
  （上記参照）。今後同種のファイルが紛れ込んだら同様に無視してよい。
